<?xml version="1.0" encoding="utf-8"?>
<!--
  infra/local/packer/http-windows/Autounattend.xml

  Unattended install answer file for Windows Server 2022 (Standard
  Evaluation, Desktop Experience). Attached via Packer's cd_files as a
  "cidata"-labeled disc alongside bootstrap.ps1 — see
  infra/local/packer/windows-server.pkr.hcl.

  Computer name is left as a generic placeholder (WIN-TEMP) rather than
  templated per-VM here: config/dc and config/member's Ansible roles rename
  the host as part of domain promotion/join, which is the point at which
  the name actually matters. Keeping this file identical for dc01 and
  mem01 builds means one Packer template serves both (parameterized only
  by var.vm_name for the output artifact, not the guest's transient
  pre-domain-join name).

  STATUS: written but not build-tested — this machine has no local
  qemu/packer install to validate against (see BUILD_LOG.md). Treat as a
  careful first draft; validate before relying on it.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend" xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <settings pass="windowsPE">
    <component name="Microsoft-Windows-International-Core-WinPE" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>
    <component name="Microsoft-Windows-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <DiskConfiguration>
        <!-- Single MBR primary partition, not the EFI/MSR/Primary GPT
             scheme this originally had. windows-server.pkr.hcl builds
             under QEMU's "pc" machine + tcg accelerator — legacy BIOS,
             no EFI firmware attached (see that file's accelerator
             comment) — and Windows Setup hard-refuses to install a
             GPT-partitioned disk under legacy BIOS ("Windows cannot be
             installed to this disk. The selected disk is of the GPT
             partition style.", observed on a real build attempt — see
             BUILD_LOG.md session 4). BIOS boot needs MBR + one active
             partition, not GPT + an ESP. -->
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>
          <CreatePartitions>
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>
          <ModifyPartitions>
            <ModifyPartition wcm:action="add">
              <Order>1</Order>
              <PartitionID>1</PartitionID>
              <Label>Windows</Label>
              <Letter>C</Letter>
              <Format>NTFS</Format>
              <Active>true</Active>
            </ModifyPartition>
          </ModifyPartitions>
        </Disk>
      </DiskConfiguration>
      <ImageInstall>
        <OSImage>
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/NAME</Key>
              <Value>Windows Server 2022 SERVERSTANDARD</Value>
            </MetaData>
          </InstallFrom>
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>1</PartitionID>
          </InstallTo>
        </OSImage>
      </ImageInstall>
      <UserData>
        <AcceptEula>true</AcceptEula>
        <ProductKey>
          <WillShowUI>Never</WillShowUI>
        </ProductKey>
      </UserData>
    </component>
  </settings>

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <ComputerName>WIN-TEMP</ComputerName>
      <TimeZone>UTC</TimeZone>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <UserAccounts>
        <AdministratorPassword>
          <Value>__ADMIN_PASSWORD__</Value>
          <PlainText>true</PlainText>
        </AdministratorPassword>
      </UserAccounts>
      <!-- Without AutoLogon, Windows sits at the lock screen after OOBE
           forever waiting for someone to physically type credentials —
           FirstLogonCommands are tied to the first REAL interactive
           logon, so bootstrap.ps1 (which enables WinRM Basic auth) never
           runs, and Packer's WinRM communicator polls indefinitely
           against whatever default/incomplete WinRM state the base image
           ships with. Observed on a real build: Windows Setup completed
           successfully (confirmed via VNC screenshot — a real Windows
           lock screen, not stuck mid-install) while Packer kept retrying
           WinRM with persistent "401 - invalid content type" errors for
           over an hour. LogonCount=1 is enough to trigger
           FirstLogonCommands once; it does not leave the account
           auto-logging-in on every subsequent boot. -->
      <AutoLogon>
        <Enabled>true</Enabled>
        <LogonCount>1</LogonCount>
        <Username>Administrator</Username>
        <Password>
          <Value>__ADMIN_PASSWORD__</Value>
          <PlainText>true</PlainText>
        </Password>
      </AutoLogon>
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <NetworkLocation>Work</NetworkLocation>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
      </OOBE>
      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <CommandLine>powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\bootstrap.ps1</CommandLine>
          <Description>Enable WinRM so Packer/Ansible can reach this host</Description>
        </SynchronousCommand>
      </FirstLogonCommands>
    </component>
  </settings>

</unattend>
